<div class="header2">

</div>
<div class="container">
  <div class="content">
    <div class="banner">
      <div class="spacing-top">
        <h1><span class="lead">《超级行动训练营》即将起航！
          <hr>
        </span>即刻登船，到知识的海洋里打捞宝藏。</h1>
        <p>课程开放日期：3月21日开课，即日起即可报名</p>
        <h1>课程时间安排</h1>
        <p>3月20日第1课：成为超级行动派的秘密</p>
        <p>3月23日第2课：如何制定一份马上就能用的计划</p>
        <p>3月27日第3课：让你弯道超车的魔鬼进步公式</p>
        <p>3月30日第4课：阶段性爆破增值路径提取</p>
        <p>上课时间地点：线上微信群统一直播听课</p>
        <p>注意：本课程为系列课程，点开任意一个Part购买即可，不需要重复购买。</p>
      </div>
    </div>
    <%#= render "carousel" %>
    <div class="category-bar">
      <div class="spacing-top">
        <div class="search">
          <%= search_form_for @q, :builder => SimpleForm::FormBuilder, class:"search_form" do |f| %>
            <!-- <%= f.label :Search, class: "h2"%> -->
            <div class="search_form_content">
              <%= f.search_field :title_or_description_cont, placeholder: "请在此输入搜索内容", class: "search_field" %>
            </div>
            <div class="search_form_button">
              <%= f.submit "search",class: "btn btn-lg btn-info" %>
            </div>
          <% end %>
        </div>
      </div>
      <!-- nav-tabs -->
      <div class="spacing-top">
        <ul class="nav nav-tabs">
          <li class="active"><a data-toggle="all" href="#home">All</a></li>
          <% @categories.each do |c|%>
            <li><a data-toggle="tab" href="#<%= c.id %>"><%= c.name %></a> </li>
          <% end %>
        </ul>
      </div>
      <!-- End nav-tabs -->
      <div class="spacing-top">
        <!-- tab-content -->
        <div class="tab-content">
          <!-- first tab -->
          <div class="tab-pane fade in active" id="home">
            <div class="content-md col-md-12" style="text-center;">
              <div class="row">
                <% @products.each do |product| %>
                  <div class="col-xs-10 col-sm-6 col-md-6" style="margin-bottom: 30px;">
                    <%= render_product_image(product) %>
                    <%= product.title %><span style="color:#ef0568;">  ￥<%= product.price %></span>
                  </div>
                <% end %>
              </div>
            </div>
          </div>
          <!-- next tab_s -->
          <% @categories.each do |c| %>
            <div class="tab-pane fade" id="<%= c.id %>">
              <div class="content-md col-md-12" style="text-center;">
                <div class="row">
                  <% instance_variable_get("@products_#{c.id}").each do |product| %>
                    <div class="col-xs-10 col-sm-6 col-md-6 col-lg-6" style="margin-bottom: 30px;">
                      <%= render_product_image(product) %>
                      <%= product.title %><span style="color:#ef0568;">  ￥<%= product.price %></span>
                    </div>
                  <% end %>
                </div>
              </div>
            </div>
          <% end %>
        </div>
        <!-- End tab-content -->
      </div>
    </div>
  </div>
</div>
